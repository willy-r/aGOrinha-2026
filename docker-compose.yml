services:
  app1:
    image: willrodrigues/gorinha-2026:latest
    networks:
      - backend
    environment:
      - GOGC=400
      - GOMEMLIMIT=150MiB
      - KNN_WORKERS=2
    deploy:
      resources:
        limits:
          cpus: "0.45"
          memory: "165MB"
    healthcheck:
      test: ["CMD", "wget", "-q", "-O-", "http://127.0.0.1:9999/ready"]
      interval: 5s
      timeout: 3s
      retries: 20
      start_period: 30s

  app2:
    image: willrodrigues/gorinha-2026:latest
    networks:
      - backend
    environment:
      - GOGC=400
      - GOMEMLIMIT=150MiB
      - KNN_WORKERS=2
    deploy:
      resources:
        limits:
          cpus: "0.45"
          memory: "165MB"
    healthcheck:
      test: ["CMD", "wget", "-q", "-O-", "http://127.0.0.1:9999/ready"]
      interval: 5s
      timeout: 3s
      retries: 20
      start_period: 30s

  nginx:
    image: nginx:1.27-alpine
    ports:
      - "9999:9999"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    networks:
      - backend
    depends_on:
      app1:
        condition: service_healthy
      app2:
        condition: service_healthy
    deploy:
      resources:
        limits:
          cpus: "0.10"
          memory: "20MB"

networks:
  backend:
    driver: bridge
